# Variables de entorno del FRONTEND — solo valores públicos (NEXT_PUBLIC_*)
# Ninguna clave sensible (service role, JWT secret, Claude API key) va aquí.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
